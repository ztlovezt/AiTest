# TestHub平台用例生成模块拆解

## 一、模块概述

TestHub平台的AI用例生成模块是一个需求驱动的测试用例生成系统。它允许用户通过多种方式输入需求（手动输入、文档上传、知识库关联、Axure原型），然后调用大语言模型自动生成结构化的测试用例。

整个模块的核心流程可以概括为：需求输入 → AI分析 → 用例生成 → 人工评审 → 用例采纳。

---

## 二、技术架构解析

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    前端层 (Vue3 + Element Plus)               │
├─────────────────────────────────────────────────────────────┤
│  需求输入面板                                                │
│  ├─ 手动输入需求                                             │
│  ├─ 上传需求文档 (PDF/Word/TXT/Markdown)                      │
│  ├─ 知识库关联                                               │
│  └─ Axure原型解析                                            │
├─────────────────────────────────────────────────────────────┤
│                    后端层 (Django REST Framework)            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │ Document     │  │ AI Model     │  │ Test Case        │  │
│  │ Processor    │  │ Service      │  │ Generator        │  │
│  │ (文档解析)    │  │ (模型调用)    │  │ (用例生成)        │  │
│  └──────────────┘  └──────────────┘  └──────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                    AI模型层 (OpenAI兼容API)                   │
│         DeepSeek / 通义千问 / 硅基流动 / 智谱等               │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 核心数据模型

```
RequirementDocument          RequirementAnalysis
├─ title                     ├─ analysis_report
├─ file (上传文件)            ├─ requirements_count
├─ document_type             ├─ analysis_time
├─ extracted_text            └─ requirements (关联)
└─ status                    
                             BusinessRequirement
                             ├─ requirement_id
                             ├─ requirement_name
                             ├─ requirement_type
                             ├─ module
                             └─ description
```

---

## 三、核心实现细节

### 3.1 文档解析引擎

**文件位置**: `backend/apps/requirement_analysis/services.py`

```python
class DocumentProcessor:
    """多格式文档解析器"""
    
    # PDF解析 - 基于PyPDF2
    @staticmethod
    def extract_text_from_pdf(file_path: str) -> str:
        pdf_reader = PdfReader(file)
        for page in pdf_reader.pages:
            text += page.extract_text() + "\n"
    
    # Word解析 - 基于python-docx
    @staticmethod
    def extract_text_from_docx(file_path: str) -> str:
        doc = docx.Document(file_path)
        for paragraph in doc.paragraphs:
            text += paragraph.text + "\n"
```

**技术特点**:
- 支持PDF、Word、TXT、Markdown四种格式
- 自动处理编码问题（UTF-8/GBK fallback）
- 自定义存储路径（`expand/document`目录）

### 3.2 AI用例生成引擎

**文件位置**: `backend/apps/requirement_analysis/models.py` (AIModelService类)

#### 3.2.1 提示词设计

**生成指令设计**（关键代码片段）:

```python
user_message = (
    f"请深入分析以下需求文档，并设计高覆盖率的测试用例。\n\n"
    f"【生成指令】\n"
    f"1. **数量原则**：请根据需求内容的实际复杂度，自动决定生成用例的数量..."
    f"2. **深度遍历策略**：按文档结构逐章节分析，不要遗漏末尾的功能点..."
    f"3. **拒绝合并**：严禁将多个验证点合并在一条用例中..."
    f"4. **场景扩展库**：数据完整性、业务逻辑约束、外部接口异常..."
    f"5. **输出顺序要求**：必须按用例编号从小到大的顺序输出..."
)
```

**提示词设计思路**:
1. **思维链引导**：要求AI按章节逐层分析
2. **场景覆盖策略**：强制要求1个正常场景 + 2-3个异常/边界场景
3. **格式约束**：禁止Markdown加粗、管道符转义处理（`&#124;`）
4. **输出规范**：连续编号、禁止跳号、完整输出

#### 3.2.2 流式生成与自动续写

```python
@staticmethod
async def call_openai_compatible_api_stream(
    config: AIModelConfig,
    messages: List[Dict[str, str]],
    callback=None
) -> AsyncIterator[str]:
    """
    流式调用OpenAI兼容格式的API，支持自动续写
    """
    # 续写控制 - 防止生成长文本时截断
    continuation_count = 0
    MAX_CONTINUATIONS = 5  # 最大续写次数
    
    while continuation_count <= MAX_CONTINUATIONS:
        # 检测finish_reason是否为'length'（截断）
        if finish_reason == 'length':
            # 自动续写：将已生成内容加入历史，继续请求
            current_messages.append({
                "role": "user", 
                "content": "请继续输出剩余的内容..."
            })
            continuation_count += 1
```

**解决的问题**: 大模型生成长文档时因token限制导致的截断问题

### 3.3 多模型适配层

**温度参数自适应**:
```python
# 特殊模型强制temperature映射
special_model_temperature_map = {
    'kimi-2.5': 1.0,      # Moonshot Kimi只支持temperature=1
    'kimi-k2.5': 1.0,
    'kimi': 1.0,
}

# 通用模型使用配置值
final_temperature = model_config.get('temperature', 0.0)
```

**API路径智能补全**:
```python
# 自动补全/v1/chat/completions路径
if not base_url.endswith('/chat/completions'):
    version_match = re.search(r'/v(\d+)/?$', base_url)
    if version_match:
        url = f"{base_url}/chat/completions"
    else:
        url = f"{base_url}/v1/chat/completions"
```

---

## 四、实践案例：电商订单系统用例生成

### 4.1 测试场景

假设我们要为一个电商订单系统生成测试用例，需求描述如下：

```
订单管理模块需求：

1. 订单创建
   - 用户可以将购物车商品提交为订单
   - 支持选择收货地址
   - 支持选择优惠券
   - 自动计算订单金额（商品总价 + 运费 - 优惠）
   - 库存校验：商品库存不足时无法提交

2. 订单支付
   - 支持支付宝、微信支付
   - 支付超时时间为30分钟
   - 支付成功后订单状态变为"待发货"
   - 支付失败保留订单，状态为"待支付"

3. 订单查询
   - 用户可以查看历史订单列表
   - 支持按订单状态筛选
   - 支持按时间范围筛选
   - 订单详情页显示物流信息
```

### 4.2 系统处理流程

**第一步：需求输入**

在TestHub平台的"需求分析"页面，选择"手动输入需求"面板，填写：
- 需求标题：电商订单管理系统
- 需求描述：上述需求文本
- 关联项目：选择对应的测试项目

**第二步：AI分析与用例生成**

系统调用配置的AI模型（如DeepSeek-V3），按照以下流程处理：

1. **需求解析**：AI分析需求文本，识别出3个核心功能模块
2. **场景拆解**：每个功能模块拆解为正常场景、异常场景、边界场景
3. **用例生成**：生成结构化的测试用例表格

**第三步：生成的用例示例**

| 用例编号 | 用例标题 | 优先级 | 前置条件 | 测试步骤 | 预期结果 |
|---------|---------|-------|---------|---------|---------|
| TC001 | 正常创建订单 | P0 | 用户已登录，购物车有商品 | 1.进入购物车 2.点击结算 3.选择地址 4.选择优惠券 5.提交订单 | 订单创建成功，跳转支付页 |
| TC002 | 库存不足时提交订单 | P1 | 商品A库存为1，用户购物车有2件 | 1.添加2件商品A到购物车 2.进入结算页 3.提交订单 | 提示"库存不足"，订单提交失败 |
| TC003 | 未选择地址提交订单 | P1 | 用户已登录，无默认地址 | 1.进入结算页 2.不选择地址直接提交 | 提示"请选择收货地址" |
| TC004 | 优惠券使用验证 | P1 | 用户有可用的满100减20优惠券 | 1.添加满100元商品 2.选择优惠券 3.提交订单 | 订单金额正确减去20元 |
| TC005 | 支付超时处理 | P2 | 订单已创建，等待支付 | 1.创建订单 2.等待30分钟不支付 | 订单自动关闭，库存释放 |
| TC006 | 支付失败处理 | P1 | 订单状态为"待支付" | 1.进入订单详情 2.点击支付 3.模拟支付失败 | 订单状态保持"待支付"，可重新支付 |
| TC007 | 订单列表筛选 | P2 | 用户有多个历史订单 | 1.进入订单列表 2.选择"待发货"筛选 | 列表只显示待发货订单 |

### 4.3 用例评审与采纳

在"生成的测试用例"列表页面，可以看到：

```
┌─────────────────────────────────────────────────────────────┐
│ 任务ID: TASK-20250325-001                                   │
│ 需求名称: 电商订单管理系统                                    │
│ 状态: 已完成                                                │
│ 生成用例数: 12条                                            │
│ 生成时间: 2025-03-25 14:32:18                               │
├─────────────────────────────────────────────────────────────┤
│ [查看详情] [批量采纳] [批量弃用]                              │
└─────────────────────────────────────────────────────────────┘
```

点击"批量采纳"后，系统会将这些用例导入到正式测试用例库，测试人员可以：
- 编辑用例标题和描述
- 调整优先级
- 关联到具体项目和版本
- 补充测试数据

### 4.4 实际效果评估

**覆盖率分析**:

| 覆盖维度 | 评估结果 | 说明 |
|---------|---------|------|
| 功能覆盖 | 85% | 主要功能点都有覆盖，但缺少部分边界条件 |
| 异常场景 | 70% | 常规异常有覆盖，复杂业务异常较少 |
| 数据完整性 | 60% | 需要人工补充具体测试数据 |
| 可执行性 | 75% | 步骤描述较清晰，但部分前置条件不够具体 |

**生成质量评分**: 7.5/10

**主要问题**:
1. 部分用例步骤过于概括，缺少具体输入值
2. 缺少性能测试相关用例
3. 安全测试场景覆盖不足

---

## 五、优缺点分析

### 5.1 做得好的地方

#### 5.1.1 架构设计

| 维度 | 具体表现 | 技术实现 |
|-----|---------|---------|
| 模块化 | 需求分析、用例生成、用例管理三个模块解耦 | 独立的Django App划分 |
| 可扩展 | 支持多模型热插拔 | AIModelConfig动态配置 |
| 容错性 | 生成失败有降级方案 | 模拟数据fallback机制 |
| 可观测 | 任务状态实时追踪 | 任务状态机设计 |

#### 5.1.2 工程实践亮点

1. **流式输出支持**
   - 用户可以看到AI实时生成内容
   - 支持自动续写，解决长文本截断问题

2. **多模型适配**
   - 统一的OpenAI兼容接口封装
   - 自动处理不同模型的参数差异

3. **灵活的输入方式**
   - 手动输入、文档上传、知识库、Axure原型四种方式
   - 满足不同场景的需求输入需求

4. **完整的用例生命周期**
   - 生成 → 评审 → 采纳/弃用
   - 支持批量操作，提高效率

### 5.2 存在的不足

#### 5.2.1 生成质量方面

| 问题 | 具体表现 | 影响程度 |
|-----|---------|---------|
| **规则生成为主** | 当前实现以模板规则为主，AI生成仅作为增强 | 高 |
| **缺乏领域知识** | 无法识别业务特定的测试场景 | 中 |
| **用例深度不够** | 步骤描述较概括，缺少具体测试数据 | 中 |
| **场景覆盖不全** | 边界条件、性能测试、安全测试覆盖不足 | 中 |

#### 5.2.2 系统功能方面

1. **缺乏用例执行闭环**
   - 生成的用例无法直接转为可执行脚本
   - 建议：增加用例→脚本转换器（如转为Playwright/Selenium代码）

2. **评审机制简单**
   - 当前评审基于规则（标题长度、步骤长度）
   - 建议：引入真正的AI评审模型，评估用例质量

3. **无持续学习机制**
   - 系统不会从执行结果中学习优化
   - 建议：增加反馈循环，根据采纳率调整生成策略

4. **缺少测试数据生成**
   - 用例步骤中缺少具体的测试数据
   - 建议：增加测试数据智能生成功能

---

## 六、总结与建议

### 6.1 技术成熟度评估

| 模块 | 成熟度 | 说明 |
|-----|-------|------|
| 需求文档解析 | 8/10 | 多格式支持稳定，文本提取准确 |
| AI用例生成 | 6/10 | 提示词工程完善，但规则生成占比高 |
| 多模型适配 | 9/10 | 适配层设计优秀，扩展性强 |
| 用例管理 | 7/10 | 基础功能完整，缺少高级筛选 |

### 6.2 改进建议

**短期（1-3个月）**:
1. 接入真正的LLM用例生成（替换当前规则生成）
2. 增加测试数据智能生成功能
3. 完善用例评审AI模型

**中期（3-6个月）**:
1. 引入RAG增强领域知识
2. 建立用例质量评估体系
3. 实现用例→脚本自动转换

**长期（6-12个月）**:
1. 构建测试知识图谱
2. 实现持续学习机制
3. 支持基于历史用例的相似推荐

### 6.3 结语

TestHub平台的AI用例生成模块展现了一个务实的工程化思路：
- 通过**模块化设计**实现功能解耦
- 通过**多模型适配**保证技术灵活性
- 通过**完整的生命周期管理**满足实际业务需求

虽然在智能化程度上还有提升空间，但其**架构设计和工程实现**为测试领域的AI应用提供了一个可用的参考实现。对于想要引入AI辅助测试用例生成的团队来说，这个模块的设计思路和实现细节都值得借鉴。

---

*本文基于TestHub平台开源代码分析，仅用于技术学习交流*
