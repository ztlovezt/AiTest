# ================================
# 服务端口配置
# ================================
# 前端服务端口（开发环境）
VITE_FRONTEND_PORT=3000
# 后端服务端口（开发环境）
BACKEND_PORT=8000
# 宿主机访问MySQL的端口（用于本地开发）
DB_PORT=3306
# 容器内部访问MySQL的端口（Docker网络内部通信，Docker部署时使用）
DB_INTERNAL_PORT=3306

# ================================
# Docker镜像配置 - Docker Hub 示例
# 不用docker部署可以忽略
# ================================
# Docker Hub 配置 (留空或使用 docker.io)
DOCKER_REGISTRY=https://hub.docker.com/
DOCKER_NAMESPACE=your-namespace
IMAGE_VERSION=latest

# ================================
# Django配置
# ================================
# Django密钥（生产环境请使用强密钥）
SECRET_KEY=your-secret-key-here-change-in-production
# 调试模式（生产环境请设为False）
DEBUG=True
# 允许的主机列表（生产环境请指定具体域名）
ALLOWED_HOSTS=*
# 允许的跨域来源（多个用逗号分隔）
CORS_ALLOWED_ORIGINS=http://localhost,http://127.0.0.1

# ================================
# 数据库配置
# ================================
# 数据库名称
DB_NAME=testhub
# 数据库用户名
DB_USER=root
# 数据库密码
DB_PASSWORD=your-database-password

# ================================
# Redis 配置
# ================================
# Redis 连接 URL（用于 Celery、Channels 和缓存）
REDIS_URL=redis://127.0.0.1:6379/0

# ================================
# 邮件配置（可选,用于定时任务通知）
# ================================
EMAIL_HOST=smtp.163.com
EMAIL_PORT=465
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_email_password
DEFAULT_FROM_EMAIL=your_email@gmail.com

# 日志配置
# LOG_LEVEL=INFO
# LOG_FILE=/app/logs/app.log

# 本地化配置
# 语言: 'en-us' (英语), 'zh-hans' (简体中文), 'ja' (日语), 'ko' (韩语) 等
# 时区: 参考 https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
LANGUAGE_CODE=zh-hans
TIME_ZONE=Asia/Shanghai

# ================================
# 生产环境注意事项
# ================================
# 1. 请务必修改SECRET_KEY为强密钥
# 2. 设置DEBUG=False
# 3. 在ALLOWED_HOSTS中指定具体域名，不要使用*
# 4. 在CORS_ALLOWED_ORIGINS中指定具体的前端域名
# 5. 使用环境变量管理敏感信息，不要提交到版本控制
# 6. 确保数据库密码足够强壮
# 7. 邮件配置使用真实的SMTP服务