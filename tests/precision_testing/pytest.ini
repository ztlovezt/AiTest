[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: 单元测试 (默认全部归此类)
    perf: 性能基准测试 (验证 §8.5.6 推理预算)
addopts = -ra --strict-markers --reuse-db
